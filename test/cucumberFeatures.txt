//http://www.baeldung.com/cucumber-rest-api-testing
Feature: Create a Service
	Background:
		Given there is a Service server
	
	Scenario: Add a Service
		Given I have a Service payload
		When I POST it to the /services endpoint
		Then I receive a 201 status code
		And I receive the identifier of my Service
		
Feature: Delete a Service
	Background:
		Given there is a Service server
		And the Service server contains my Service
	
	Scenario: Delete my Service
		Given I have my Service identifier
		When I send a DELETE to the /service/{id} endpoint
		Then I receive a 200 status code
		And when I GET /service/{id}
		Then I receive a 404 response code
		
Feature: Create a maintenance warning
	Background:
		Given there is a Service server
		And the Service server contains my Service

	Scenario: Submit a maintenance warning
		Given I have my Service identifier
		And I have a Maintenance payload
		When I POST it to the /service/{id}/maintenance
		Then I receive a 201 status code
		
Feature: Check Services
	Background:
		Given there is a Service server

	Scenario: Check if the Service is up and running
		Given I have a Service identifier
		When I GET /service/{id}
		Then I receive a payload containing the Service's status
		
	Scenario: Check multiple Services
		When I GET /services
		Then I receive a payload containing all Services